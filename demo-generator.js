#!/usr/bin/env node

console.log('🚀 CloudIDE Demo - Website Generator (No API Key Required!)');
console.log('==============================================================');

// Simple website templates
const templates = {
    technology: {
        title: 'TechVision Pro',
        description: 'Revolutionary technology solutions that transform ideas into reality',
        tagline: 'Building the Future',
        features: ['AI-Powered Solutions', 'Cloud Infrastructure', 'Mobile Development', 'Data Analytics']
    },
    business: {
        title: 'EliteEnterprise',
        description: 'Professional business solutions for modern enterprises',
        tagline: 'Excellence in Business',
        features: ['Strategic Planning', 'Process Optimization', 'Team Management', 'Growth Analytics']
    },
    restaurant: {
        title: 'Gourmet Garden',
        description: 'Exceptional dining experience with fresh, locally sourced ingredients',
        tagline: 'Taste the Difference',
        features: ['Farm-to-Table', 'Chef Specials', 'Private Events', 'Online Ordering']
    }
};

function generateWebsite(prompt) {
    const lowerPrompt = prompt.toLowerCase();
    let industry = 'business';
    
    if (lowerPrompt.includes('tech') || lowerPrompt.includes('startup') || lowerPrompt.includes('software')) {
        industry = 'technology';
    } else if (lowerPrompt.includes('restaurant') || lowerPrompt.includes('food')) {
        industry = 'restaurant';
    }
    
    const template = templates[industry];
    
    const html = `<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>${template.title}</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 1200px; margin: 0 auto; padding: 0 20px; }
        header { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; padding: 4rem 0; text-align: center; }
        h1 { font-size: 3.5rem; margin-bottom: 1rem; }
        .subtitle { font-size: 1.3rem; margin-bottom: 2rem; opacity: 0.9; }
        .cta { background: #ff6b6b; color: white; padding: 15px 40px; text-decoration: none; border-radius: 50px; font-weight: bold; display: inline-block; }
        .features { padding: 4rem 0; background: #f8f9fa; }
        .features h2 { text-align: center; margin-bottom: 3rem; font-size: 2.5rem; color: #333; }
        .feature-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 2rem; }
        .feature { background: white; padding: 2rem; border-radius: 15px; box-shadow: 0 8px 25px rgba(0,0,0,0.1); text-align: center; }
        .feature h3 { color: #667eea; margin-bottom: 1rem; font-size: 1.3rem; }
        footer { background: #333; color: white; text-align: center; padding: 3rem 0; }
        .generated-badge { background: #28a745; color: white; padding: 5px 15px; border-radius: 20px; font-size: 0.9rem; margin-top: 2rem; display: inline-block; }
    </style>
</head>
<body>
    <header>
        <div class="container">
            <h1>${template.title}</h1>
            <p class="subtitle">${template.description}</p>
            <a href="#features" class="cta">Explore Features</a>
            <div class="generated-badge">✨ Generated by CloudIDE - No API Key Required!</div>
        </div>
    </header>
    
    <section id="features" class="features">
        <div class="container">
            <h2>Our Features</h2>
            <div class="feature-grid">
                ${template.features.map(feature => `
                    <div class="feature">
                        <h3>${feature}</h3>
                        <p>Professional ${feature.toLowerCase()} services designed to exceed your expectations and drive success.</p>
                    </div>
                `).join('')}
            </div>
        </div>
    </section>
    
    <footer>
        <div class="container">
            <p>&copy; 2024 ${template.title} - ${template.tagline}</p>
            <p style="margin-top: 1rem; opacity: 0.8;">Generated in seconds by CloudIDE's intelligent fallback system</p>
        </div>
    </footer>
</body>
</html>`;

    return {
        id: Date.now().toString(),
        title: template.title,
        industry: industry,
        html: html,
        timestamp: new Date().toISOString(),
        source: 'cloudide-fallback'
    };
}

// Demo the generator
const prompts = [
    "Create a modern tech startup website",
    "Build a professional business consultancy site", 
    "Make a restaurant website with online ordering"
];

console.log('\n🎨 Generating sample websites...\n');

prompts.forEach((prompt, index) => {
    console.log(`${index + 1}. Prompt: "${prompt}"`);
    const website = generateWebsite(prompt);
    console.log(`   ✅ Generated: ${website.title} (${website.industry})`);
    console.log(`   🆔 ID: ${website.id}`);
    console.log(`   📏 HTML Size: ${website.html.length} characters`);
    console.log('');
});

console.log('🎉 SUCCESS! Your CloudIDE can generate websites WITHOUT any API keys!');
console.log('');
console.log('📊 Summary:');
console.log('• ✅ No OpenAI API key required');
console.log('• ✅ Intelligent industry detection'); 
console.log('• ✅ Professional responsive designs');
console.log('• ✅ Multiple website types supported');
console.log('• ✅ Instant generation (no API calls)');
console.log('• ✅ Unlimited free usage');
console.log('');
console.log('💡 To get even better results with AI:');
console.log('   1. Get free $5 credits at https://platform.openai.com/signup');
console.log('   2. Add your API key to backend/.env');
console.log('   3. Enjoy 1,000-3,000 AI-powered website generations');
console.log('');
console.log('🚀 Your CloudIDE is ready to deploy to Vercel whenever you want!');
